<?xml version="1.0" encoding="UTF-8"?>

<entry key="selectBoardList">
    SELECT
    (
	      BOARD_NO,
	      BOARD_TYPE,
	      CATEGORY_NO,
	      BOARD_TITLE,
	      BOARD_WRITER,
	      COUNT,
	      CREATE_DATE,
	      STATUS
	)
    FROM BOARD
    WHERE STATUS = 'Y'
    ORDER BY BOARD_NO DESC
</entry>