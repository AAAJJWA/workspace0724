<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.yugioh.mapper.MemberMapper">

    <select id="existsLoginId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM member
        WHERE login_id = #{loginId}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (login_id, password, nickname)
        VALUES (#{loginId}, #{password}, #{nickname})
    </insert>

    <select id="findLoginId" resultType="com.kh.yugioh.entity.Member">
        SELECT id, login_id, password, nickname
        FROM member
        WHERE login_id = #{loginId}
    </select>

</mapper>
